import * as  React from 'react'
import { Navbar } from '..'

const Hero = () => {
  return (
      <header className='hero-wrapper w-full h-screen overflow-hidden'>
          <Navbar />
    </header>
  )
}

export default Hero